# Built/downloaded app artifacts — large binaries, reproducible from documented URLs
*.war
*.tar.gz
*.zip
postgresql.jar
apache-roller-*/
# Exploded webapp dirs + runtime stores
*/webapp/
*/pages/
*/work/
*/roller_data/
# Generated by roller/setup.sh: the DB seed (Roller's own DDL + generated INSERTs) and the
# container-provided jars it downloads. Both are reproducible — re-run setup.sh.
roller/seed/
roller/tomcat-lib/
