# Deploy a javax.servlet WAR on Tomcat 9 with the Basquin valve attached to every
# request. The same basquin-valve jar is used as the Tomcat 10 path — its bytecode is
# namespace-free, so it loads on both (see docs/DESIGN-DECISIONS.md DD-011).
#
# WAR_PATH is required (no demo WAR is javax). Example — JPetStore built locally:
#   ./gradlew jar :tomcat-valve:jar
#   WAR_PATH=/abs/jpetstore-6/target/jpetstore.war docker compose -f docker-compose.valve9.yml up tomcat9-valve
#
services:
  tomcat9-valve:
    image: tomcat:9.0-jdk17-temurin
    ports:
      - "${TOMCAT_HOST_PORT:-8080}:8080"
    environment:
      - CATALINA_OPTS=-javaagent:/usr/local/tomcat/basquin-agent.jar -Xbootclasspath/a:/usr/local/tomcat/basquin-agent.jar -Dbasquin.invariant.mode=${INVARIANT_MODE:-soft} -Dbasquin.invariant.latency.maxMs=${INVARIANT_LATENCY_MS:-50} -Dbasquin.invariant.latency.sample=${INVARIANT_LATENCY_SAMPLE:-true} -Dbasquin.invariant.heapDelta.maxKb=${INVARIANT_HEAP_KB:-256} ${CATALINA_OPTS_EXTRA:-}
    volumes:
      - ${WAR_PATH:?set WAR_PATH to a javax.servlet WAR}:/usr/local/tomcat/webapps/ROOT.war:ro
      - ./build/libs/basquin-0.2.0.jar:/usr/local/tomcat/basquin-agent.jar:ro
      - ./tomcat-valve/build/libs/basquin-valve-0.1.0.jar:/usr/local/tomcat/lib/basquin-valve.jar:ro
      - ./deploy/valve/context.xml:/usr/local/tomcat/conf/context.xml:ro
