# Allowlist for scripts/check-citations.py. Every entry MUST carry a reason — an unexplained
# exemption is exactly the claim/check drift the tool exists to stop. Three entry kinds:
#   <cited-path-or-prefix/> <reason>       exempt a cited path (prefix if it ends with /)
#   frozen <citing-prefix> <reason>        do not scan this citing file/dir at all
#   reported <citing-prefix> <needle> <reason>  suppress-but-print findings in <citing-prefix>
#                                          containing <needle>: real defects in files owned by
#                                          other agents, reported 2026-07-30, pending their fix.

# --- deliberately absent (proposed, not yet built) ---
bench-results/RUN-OF-RECORD proposed in DD-045 item 2 (docs/ROADMAP.md); deliberately not built yet — forward-looking reference, not a broken one

# --- upstream repos: files that live in the target application's tree, not ours ---
verify.yaml apicurio-registry's CI workflow (upstream repo), cited as evidence context
verify-cli.yaml apicurio-registry upstream workflow
verify-build.yaml apicurio-registry upstream workflow
verify-unit-tests.yaml apicurio-registry upstream workflow
verify-extras.yaml apicurio-registry upstream workflow
verify-integration-tests.yaml apicurio-registry upstream workflow
verify-publish.yaml apicurio-registry upstream workflow
release.yaml apicurio-registry upstream workflow
native-images-tests.yml apicurio-registry upstream workflow
cross-maven.yml apicurio-registry upstream workflow
.github/workflows/native-images-tests.yml apicurio-registry's workflow tree, not this repo's
.github/workflows/cross-maven.yml apicurio-registry's workflow tree, not this repo's
.github/workflows/README.md apicurio-registry's workflow tree, not this repo's
infra.yml quarkus rest-villains upstream compose file
java25.yml quarkus rest-villains upstream compose file
deploy/db-init/initialize-tables.sql quarkus rest-villains upstream path (collides with our deploy/ top dir but is theirs)
WeakHashMap.java JDK source (java.util), cited in the jspwiki incident analysis

# --- external or generated config that never lives in this tree ---
settings.xml Maven per-user/container settings, outside any repo tree by definition
maven-metadata.xml Maven repository metadata, generated by publishing (lives on the Pages repo / injected repo)
server.xml Tomcat container configuration, mounted at run time
roller-custom.properties Roller target-app config file inside its container image
jspwiki-custom.properties JSPWiki target-app config file inside its container image
quarkus-extension.yaml Quarkus extension metadata generated inside the extension jar
reflect-config.json GraalVM native-image agent output, generated per build
jacoco-agent.properties JaCoCo agent config, generated/classpath-internal
basquin-injector.properties baked into the injector jar at build time (see verifyBakedVersion), never a tree file
corpus.txt runner output written into the run volume at run time
run.json runner per-run output artifact, generated at run time
k6.json k6 load-run output artifact, generated at run time

# --- disclosed-dead references in docs that catalogue them (the prose is ABOUT the deadness,
#     but table rows put the disclosure outside the tool's +/-1-line negation window) ---
dd039-spike-report.md named in TODO.md's audit table OF dead .superpowers citations; disclosed dead two lines below the table
jspwiki-save-rootcause.md named in TODO.md's audit table of dead .superpowers citations; disclosed dead there
jspwiki-runner-save-rootcause.md named in TODO.md's audit table of dead .superpowers citations; disclosed dead there
dcv-guard-verify-list.log rerun.sh output, generated on demand; the r4 README discloses it is "not part of the original 17" committed logs
dsc-guard-verify-list.log rerun.sh output, generated on demand; disclosed as not committed (r4 README)
dprov-guard-verify-list.log rerun.sh output, generated on demand; disclosed as not committed (r4 README)

# --- frozen citing files: historical plans/specs for shipped work. The 2026-07-30 audit
#     (bench-results/dd043-pr3-citation-audit-2026-07-30/) deliberately left their 100+ stale
#     citations unfixed: they describe the tree at each document's base commit, and repointing
#     them would launder false-looking-true statements. NOTE: this means citations in these
#     files are UNCHECKED — a conscious exclusion, not coverage. ---
frozen docs/superpowers/plans/ historical point-in-time execution plans; audit left their stale citations deliberately (119 of 135 wrong sat in this class)
frozen docs/superpowers/specs/2026-07-21-operator-server-side-boundary-design.md historical design doc for shipped work; same audit rationale
frozen docs/superpowers/specs/2026-07-23-trustworthy-measurement-design.md historical design doc for shipped work; same audit rationale
frozen docs/superpowers/specs/2026-07-24-native-reactive-targets-design.md historical design doc for shipped work; same audit rationale

# --- real defects found by this tool 2026-07-30 in files owned by other agents; reported with
#     corrections in .superpowers/sdd/pr103-dd045-checker-report.md, suppressed here so the
#     gate can land, visible in every run until the owner fixes them. (Two earlier entries —
#     verify-dd043-pr3.sh's superseded `782,-767,9|0||` quote and TODO.md's carried
#     `20260730T054112Z`@`9f1e990` stamp — were fixed by their owners mid-session and the
#     entries removed.) ---
reported docs/DESIGN-DECISIONS.md agents.md cites an agents.md instruction file that is not in the tree (authoring-machine local); owner should inline the rule or point at a committed file
